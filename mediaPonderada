#include <stdio.h>
 
int main() {
 
    double a, b, pesoA = 3.5, pesoB = 7.5, media;
    scanf("%lf", &a);
    scanf("%lf", &b);
    
    media = ((a*pesoA + b*pesoB)/11); //valor 11 é a soma da média ponderada
    printf("MEDIA = %.5lf\n", media);
 
    return 0;
}
