#include <stdio.h>
 
int main() {
 
    double a, b, c, pesoA = 2, pesoB = 3, pesoC = 5, media;
    scanf("%lf", &a);
    scanf("%lf", &b);
    scanf("%lf", &c);
    
    media = ((a*pesoA + b*pesoB + c*pesoC)/10);
    printf("MEDIA = %.1lf\n", media);
 
    return 0;
}
